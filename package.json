{
  "name": "cliente",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "debug": "tsnd --respawn --preserve-symlinks --exit-child --inspect=0.0.0.0  -- src/main/server.ts",
    "start": "tsnd --respawn --transpile-only --ignore-watch node_modules --no-notify --exit-child src/main/server.ts",
    "db:migrate": "npx prisma migrate dev --create-only --name ",
    "db:generate": "npx prisma generate",
    "db:push": "npx prisma db push",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@prisma/client": "^4.12.0",
    "@types/cors": "^2.8.13",
    "@types/express": "^4.17.17",
    "@types/node": "^18.15.11",
    "@types/uuid": "^9.0.1",
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "prisma": "^4.12.0",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.0.2",
    "uuid": "^9.0.0"
  },
  "devDependencies": {
    "@mermaid-js/mermaid-cli": "^10.0.2",
    "prisma-erd-generator": "^1.5.4"
  }
}
